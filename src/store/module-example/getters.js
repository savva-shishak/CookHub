export const getKitchens = ({ menu: { kitchens } }) => {
    return kitchens
}

export const getCaptions = ({ menu: { captions } }) => {
    return captions
}

export const getTypes = ({ menu: { types } }) => {
    return types
}

export const getUser = ({ user }) => {
    return user
}

export const getRecipes = ({ recipes }) => {
    return recipes
}
